# Docker network overrides (loaded after backend/.env).
# Copy to docker/stack.env — must match docker/postgres.env credentials.
DATABASE_URL=postgresql+asyncpg://securechat:changeme@postgres:5432/securechat
REDIS_URL=redis://redis:6379/0
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001
